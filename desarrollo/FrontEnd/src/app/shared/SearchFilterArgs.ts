import { PaginationOptions } from "./PaginationOptions";

export class SearchFilterArgs {
  /* Opciones para la paginación */
  paginationOptions?: PaginationOptions;
  /* Filtro de busqueda para el componente de búsqueda */
  SearchFilter?: any;
}